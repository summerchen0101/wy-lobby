# Netlify：在 Site configuration → Environment variables 設定下方需內嵌進前端的變數（VITE_*）；值依環境自訂。純本機的 VITE_DEV_PROXY 等上線可省略。
#
# 可選：檔期主題 id，對應 theme-seasonal.css 的 [data-theme="…"]（不設則用預設 palette）
# VITE_APP_THEME=cny

# 後端 API 根（不帶尾隨斜線）。本機可透過 Vite proxy 或指向 staging。
VITE_API_BASE=

# 設為 true 則不呼叫真實 API，用內建假資料（僅供本機介面與殼層測試）
# 切回非 mock 時，瀏覽器內舊 token 可能僅適用 mock，建議先登出或以真實後端重新登入；否則 WS 可能連線或 SERVER_LOGIN 失敗。
VITE_API_USE_MOCK=true

# 可選：啟用 dev server 時把 /api 代理到此網域（不設則不啟用 proxy，亦可用 VITE_API_BASE 直連）
# VITE_DEV_PROXY=https://api.example.com

# 遊戲若未單獨指定 openInNewWindow，則用此：true=預設新分頁
VITE_OPEN_GAMES_IN_NEW_WINDOW=false

# 寫入 app_meta 之 version 字串（不設則 0000）
# VITE_APP_VERSION=0000

# 以下可選，覆寫預設 REST 相對路徑（以 VITE_API_BASE 為前綴）。預設已為 v1（見 docs/api-spec.md）
# VITE_API_PATH_AUTH_REGISTER=/api/v1/signup
# VITE_API_PATH_AUTH_LOGIN=/api/v1/login
# VITE_API_PATH_AUTH_TOKEN=/api/v1/token
# access 到期前幾秒主動 POST /api/v1/token（需登入／refresh 回應含 expiresIn；預設 0，僅在到期時 refresh）
# VITE_TOKEN_REFRESH_LEAD_SEC=300
# 測試：設成極大值（如 99999999）且已登入、wynoco_access_expires_at 正確 → 重整頁面應立刻打 /api/v1/token
# 或 DevTools：localStorage.setItem('wynoco_access_expires_at', String(Date.now() + 120000)) → lead=300 時約 1.5 分鐘後觸發；lead=0 時須等到到期
# VITE_API_PATH_PASSWORD_RESET=/api/v1/password/reset
# VITE_API_PATH_PASSWORD_RESET_INFO=/api/v1/password/resetInfo

# 大廳「Single1 (Alpha)」內嵌試玩用 Unity WebGL URL（不設則用內建 alpha 預設）
# VITE_UNITY_DEMO_URL=https://unityweb-alpha.ffglobaltech.com/0000/Single1/index.html

# 大廳主視覺圖 URL（不設則用內建預設圖）
# VITE_LOBBY_HERO_IMAGE=https://example.com/hero.webp

# Trustpilot Business Unit ID（不設則大廳不顯示評價區塊）
# VITE_TRUSTPILOT_BUSINESS_UNIT_ID=

# 浮動「領取獎勵」按鈕導向路徑（預設 /profile）
# VITE_FLOATING_CTA_PATH=/profile

# 邀請好友：分享連結的落地網址基底（不含尾隨 /）；不設則用瀏覽器目前 origin（`window.location.origin`）
# VITE_REFERRAL_LANDING_URL=https://example.com

# Zendesk Web Widget／Messaging snippet key（Admin Center → Channels → Messaging → Web Widget）。啟用後 App 會載入 Zendesk；CSP／進階串接見 docs/zendesk-vite-spa.md
# VITE_ZENDESK_KEY=your-widget-key-here

# Gateway WebSocket 基底（建議不含 ?query；token、deviceid 由程式組裝）
# VITE_WS_URL=wss://app-us-alpha.ffglobaltech.com/ws
# WebSocket query deviceid（不設則用內建 alpha 預設 UUID）
# VITE_WS_DEVICE_ID=

# 握手失敗：每輪逾時（毫秒）、重試次數；Close code 視為拒絕憑證（auth_rejected → 請重新登入）
# VITE_WS_HANDSHAKE_TIMEOUT_MS=15000
# 單則 Gateway WS request() 逾時（毫秒）；0 關閉。預設 15000。慢網路可試 25000–30000
# VITE_WS_REQUEST_TIMEOUT_MS=15000
# PING_PONG 心跳間隔（毫秒）；<=0 關閉。預設 5000
# VITE_WS_HEARTBEAT_INTERVAL_MS=5000
# 大廳 LOBBY_GET 輪詢間隔（毫秒）；未設時為 request 逾時 + 5000，避免與進行中請求重疊
# VITE_WS_LOBBY_GET_POLL_MS=20000
# VITE_WS_MAX_HANDSHAKE_ATTEMPTS=6
# 若後端以自訂 WebSocket Close code 拒絕 token（逗號分隔）；alpha 常見為 1006，前端另以「從未 open」判定 auth_rejected
# VITE_WS_AUTH_FAILURE_CLOSE_CODES=4401
# Gateway 業務 code 視為 session 失效（逗號分隔）；預設 401,403,401001
# VITE_WS_SESSION_INVALID_CODES=401,403,401001

# 每則 Gateway WS 請求／回應在 console 輸出摘要（RequestBasic.token 會遮罩）。dev 模式下預設與 `VITE_DEV_CONSOLE` 一致（見下）；`vite build` 預設關閉；設為 true 可於 preview／自訂環境強制開啟（不受 `VITE_DEV_CONSOLE=false` 影響）。
# VITE_GATEWAY_WS_TRACE=true

# 應用程式開發診斷 console（`[gateway-ws][dev]`、`[game-shell][dev]`、`[lobby]` 等）。未設時：`yarn dev` 開、`vite build` 關；`false` 可於本機關閉；`true` 可於 staging／preview 強制開啟。
# 若仍要看 Gateway 二進位往返，請設 `VITE_GATEWAY_WS_TRACE=true`。
# VITE_DEV_CONSOLE=false

# 大廳改走 Unity WebEntry（index.html?game_id=&mode=&amount=&vip_lv=&token=）
# mode: GC=1、SC=2；amount=目前錢包餘額；vip_lv=VIP；token=已登入 IAM token（試玩／未登入不帶）
# VITE_USE_SLOT_WEBENTRY=true
# VITE_UNITY_WEBENTRY_URL=https://unityweb-alpha.ffglobaltech.com/0001/WebGL_Build_Alpha/index.html
# VITE_UNITY_WEBENTRY_GAME_ID=85

# 僅 npm run dev：大廳自動連線 Gateway WS 並在 console 打出 state/response（設為 false 可關閉）
# VITE_DEV_GATEWAY_WS=false

# 僅 dev：連上 WS 後是否自動送 LOBBY_GET（設為 false 可關閉）
# VITE_DEV_LOBBY_GET=false

# Gateway RequestBasic.clientVer（不設則用 web-alpha）
# VITE_CLIENT_VER=web-alpha

# 非 mock：大廳遊戲列表預設來自 WebSocket LOBBY_GET；設為 false 可關閉
# VITE_USE_WS_LOBBY_GAMES=false

# 第三方遊戲入口圖基底（不帶尾隨 /）；路徑為 /{platform}/{gameUID}.png。Prod 請改用 unityweb-cdn.boss-fun.com
# VITE_THIRD_PARTY_GAME_THUMB_BASE=https://nas01.ffglobaltech.com

# `web/public/images/**` 靜態圖 CDN origin（不帶尾隨 /）。未設則沿用同源 `/images/...`。
# `vite build` 會將 apple-touch-icon 與 manifest 內 icon 改為絕對 CDN URL（有設定時）。
# VITE_PUBLIC_IMAGE_CDN_BASE=https://cdn.example.com
