# Repo root is web/; Netlify Base directory empty in UI (or same as repo root)
[build]
  command = "npm run build"
  publish = "dist"

# React Router（BrowserRouter）：子路徑與重新整理導到 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
